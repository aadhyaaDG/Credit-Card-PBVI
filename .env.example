# Copy this file to .env and adjust paths if needed.
# .env is excluded from git — never commit it.

# Absolute path to data/ inside container
DATA_DIR=/app/data

# Absolute path to source/ inside container (read-only — INV-04)
SOURCE_DIR=/app/source

# Path to dbt profiles directory inside container
DBT_PROFILES_DIR=/app/dbt_project
